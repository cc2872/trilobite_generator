# Session changes — 2026-09-13

All files are NEW. No tracked file (parts.py, schema.py, instrument.py, web/*) is modified.
Schema and hash unchanged; frozen references unaffected; instrument untouched.

## eyes/eyes2.py — standalone richer eye builder
Same frame as parts.eye_solid (z up, outward +x, base at z = -embed). Adds:
plan shape (ky, kidney), lunate taper, lens styles raised / sunken (socket + sclera rim + convex
cap) / flush, hex or grid lattice, lens diameter gradient, eyeshade brim, cheek clip (clip_x),
stalk with outward/forward lean, min_D threshold as a parameter.
Not wired into cephalon(); call directly.

## eyes/head_eyes.py — head + eyes2 eyes, four views each
Builds parts.cephalon(P) with eyeSolid=0, places eyes2 eyes at eye_geometry() (mirrored),
renders dorsal / oblique / lateral / close-up. Per-type overrides for eyeSize, eyePos, eyeLat.
Backface + frame culling in the renderer (the 460k-face tower killed matplotlib without it).

## eyes/eyes_only.py, eyes/eyes2_render.py — isolated eye grids (v1 presets, v2 family)

## eyes/renders/*.png, eyes/stl/*.stl — outputs

## scripts/read_three_presets.py
Reads proetida / asaphida / agnostida with instrument.read. NOTE the fix inside: preset JSON
wraps params under "params"; loading the file root silently coerces to the DEFAULT animal.
Recommend a loud warning in coerce_report when zero keys match.

Readings on c8469ff:
  proetida   22.81°/joint  228.1°  closed   double     gap 0.03 mm   (matches frozen ref)
  asaphida   20.70°/joint  186.3°  anatomy  open       gap 16.27 mm
  agnostida  21.41°/joint   64.2°  closed   discoidal  gap 2.86 mm   (frozen ref: invalid/rest_interference @35)

## docs/UI_ELEVATION_NOTES.md, docs/ui-elevation-sketch.svg — elevation-strip design

## Security (2026-09-15)
- parts.safe_expr: replaced eval() with an AST whitelist (_eval_node). genalPath now
  permits only numeric literals, s/pi/e, arithmetic, comparisons, and the whitelisted
  numpy functions; anything else (attribute access, calls to non-whitelisted names,
  comprehensions, lambdas, ...) is rejected -> fallback s**2 with a "genalPath rejected"
  note, same as before. Closes the RCE-via-genalPath hole. Legit formulas unchanged.
  NOTE: legacy/trilobite.py:112 still has the old eval() pattern (not served; left as-is).

## Block 1 — freeze the ruler (2026-09-15)
- References re-frozen with the mesh engine (scripts/refreeze_reading.py): agnostida and asaphida
  were old-BREP invalid/rest_interference artifacts (rest_max 10.58/9.0 -> 0.0). New readings,
  measured at the frozen bevel so the part meshes still match:
    agnostida  closed/discoidal  21.41 deg/joint  64.2 deg  gap 2.86 mm  (@35)
    asaphida   anatomy/open      20.70 deg/joint 186.3 deg  gap 16.27 mm (@45)
  redlichiida — which the plan expected to change — reproduces its frozen reading and was left.
- test_parts: test_agnostida_bevel_severs_tips_like_the_brep_builder (expected the old BREP
  severing bug) replaced by test_agnostida_builds_whole_at_both_bevels (1 watertight body at 45 and 35).
- Test suite after the refreeze: the three old-engine-bug failures now pass. Remaining failure is
  test_schema::test_prongs_build_on_head_and_tail (head prongs break L/R symmetry) — a generator
  bug, Block 2, not part of the ruler freeze.
- PREREG_v1_sweep.md: all 12 [DECISION] lines resolved to their proposed values (BOUND_DEG=45,
  SCAN_STEP=2.5, RES=0.1, REST_BUDGET=1.0, overlap ceiling 0.6 -> A2 range 0.2-0.6, null subset 50,
  N_PER_ORDER=150, CLASS_TOL=0.05L, DISC_OVERLAP=0.5, 150 deg split, MAX_COMPONENTS=3, timeouts
  300/120 s, 40% censor cutoff). Status -> RESOLVED, ready to tag prereg-v1 + Zenodo (Claire).

## manifold3d pin (2026-09-15, Block 2 start)
- Root-caused test_prongs_build_on_head_and_tail (head prongs broke L/R symmetry): NOT our code.
  manifold3d 3.5.3's Manifold.cylinder corrupts the seam vertex of each ring, (r,0)->(r,r), so
  every M.frustum spine/prong carried a spurious sqrt(2)*r spike (~0.4 mm), failing is_symmetric.
- Fixed by pinning manifold3d==3.0.1 (verified clean: 0 bad verts, prong test green). Satisfies
  PREREG §9's "requirements.txt with pinned versions". agnostida/asaphida readings are byte-identical
  on 3.0.1 vs 3.5.3 (the spike never touched their measurements), so no reference re-freeze needed.

## Still open from the 09-12 changelog (not addressed here)
- classify() discoidal branch is missing the PREREG §3 overlap term.

## 20 Sep 2026 — reconciliation of the two working copies (Claude's b1 vs the 1650 tree)
The 1650 tree and Claude's Block 1 copy had diverged from a8984dd on different lines. Kept from the 1650 tree:
safe_expr AST evaluator, manifold3d==3.0.1 pin (the 3.5.3 cylinder seam bug — note Claude's 13–20 Sep renders were
made on 3.5.3, so every frustum/cylinder in them carried the ~0.4 mm seam spike; re-verified below on 3.0.1),
refreeze_reading.py and the agnostida/asaphida re-freeze at their frozen bevels, the resolved PREREG. Brought over
from b1:
- instrument.classify: PREREG §3 overlap term (DISC_OVERLAP = 0.5), constants recorded in results. Closes the
  "still open" item at the top of this section. agnostida stays discoidal (s_tail 1.56); a short-curl closure with
  s_tail < 0.5 now falls through to double/spiral/sphaeroidal instead of being called discoidal.
- printjoint2.py — the print joint (parametrized Flexi #3839472: solid wedge segments, one barrel-in-pocket joint
  per pair). Re-verified on manifold 3.0.1: coupon stops at maxAngle, pull play = gap_axial (0.10 mm), whole default
  animal 8 single bodies, no contact through 29 deg/joint. tests/test_printjoint2.py (2 tests, 4 s).
- web/app.py: /api/build accepts "joint": "flexi" (cache key gets a -flexi suffix; manifest records the print pivot
  and gaps); /api/stl poses flexi builds with printjoint2.transforms_deg. Default stays "pin". index.html not yet
  changed (no toggle).
- scripts/printjoint2_study.py, scripts/printjoint2_sliders.py.
NOT brought over (deliberately): schema 6.2's facet/doublure keys and parts.pleural_facet / ventral_doublure. The
Block 1 gate showed them not load-bearing (|delta| <= 1.1 deg, no class flips); leaving the schema at 6.1 keeps the
frozen references and the resolved PREREG untouched. The gate evidence lives in b1's docs/block1_gate.json.
- printfill.py (20 Sep, later): print-only shell thickening for the PIN build — the layer between the dorsal surface
  and the same surface shifted down `fill` mm, hinge bands and the over-riding flap kept clear, capped at
  marginHeight*relief - 1.5 and z >= 0.6. Default animal: 26 g -> 32 g at 3 mm, single bodies, clean to its stop.
  /api/build: "fill": <mm> (pin builds only; cache key -fill<mm>; manifest fill_mm).
- printjoint2.print_head now takes the eyes from parts.eye_solid directly (was: by difference, which dropped stalks).
- OPEN (Block 2, generator): a stalked eye (eyeStalk > 0) comes out as 3 bodies in the TRACKED parts.cephalon too
  (drum + mirror floating above the surface, stalk missing) — reproduced on asaphida with eyeStalk=2.0. Not a print
  bug; the 6.1 stalk placement in cephalon() is wrong. No preset uses a stalk, so frozen references are unaffected.

## 20 September 2026 — web UI: flexi is the site default (own commit, on print-joint-and-fill)
- index.html: added the print-mode toggle beside the build/download actions (`joint · flexi` | `measure · pin`,
  plus a `mm fill` field that unlocks in pin mode). The site now builds and downloads the print-ready flexi joint
  by DEFAULT; pin gives the raw measurement shell (optionally thickened by `fill`). Every choice rebuilds, so the
  preview and the STL you download always agree. Backend default is still "pin" — the site sends the choice on each
  /api/build.
- The live 3D preview now poses flexi builds about the PRINT pivot, not the instrument hinge: pose() reads
  `print_joint.pivot_z` / `pivot_y_beyond_joint` from the manifest (same joint offsets, different pivot — mirrors
  printjoint2.transforms_deg). Measurement ("Roll") still poses about the instrument hinge.
- Untouched, deliberately: /api/measure and the sheet/blueprint still describe the 2 mm pin animal — the frozen,
  pre-registered measurement is not affected by the print default.
