
## Session 2 (8 Sep, later)
- **Wedge fixed** (`trilobite.add_hinge`): full-band wedge clipped to its own side of the hinge line; past-the-line reach kept only
  over the stop block (Wh+2). Subtracts Solids one at a time (a Compound tool made the cut fail silently). Verified one-solid,
  full-width segments at 45° for textured, phacopid, asaphid, harpetid. Every preset rebuilt with it (out10/; old wedge results in out10_v1wedge/).
- **Eyes**: `eyes.py` — `eye_geometry(P)` shared by builder, sheet and ruler; `fov(P)` FOV ruler v0.1 (declared crude: azimuth
  from eyeArc only, no occlusion; elevation = atan(eyeHeight)). New params `eyeLat` (0 = v4 glabella rule; phacopid 0.55) and
  `eyeProfile` (super-Gaussian exponent; phacopid 6). Blueprint draws the eye + arc on the plan and an FOV block in the title block.
  eye_check.png: three heads with different eye params, contours + drawn eye. KNOWN GAP: v0.1 azimuth does not move with the eyes
  dial (eyeSize/eyeHeight/glabInflate) — only eyeArc moves it. Decide before the null-axis test whether eyeArc joins the eyes macro.
- **Instrument 2.0**: collisions are now baseline-subtracted (overlap(theta) - overlap(0) > 0.5 mm^3); rest touching is by design.
- Phacopida on the fixed wedge read theta 3.9° / anatomy / seg10-tail 0.5 mm^3 (at the tolerance edge; rest overlap 0). Unresolved:
  real rib-tip-on-tail-corner contact vs tolerance noise. Re-measure with the baseline rule; if it persists, raise OVERLAP_TOL to 1.0
  with a stated reason or fix the tail's front corners.

## Session 3 (8 Sep, late) — the eye as a primitive
- `eye_solid.py` (EYE_SOLID_VERSION 1.0): revolved cap + leaning band (+ brim), lens caps on a hex lattice in (theta, s)
  within ±eyeArc/2 of outward, fused in one boolean. 87–97 lenses/eye, ~10 s per eye. Declared: full-360° body, lattice
  only trimmed to the arc; eyeElong not applied; lens axis = normal.
- schema: eyeSolid, eyeSlope, eyeShade, eyeElong, lensD, lensGap, lensRise (fossil ranges). phacopid/phacopida: eyeSolid 1,
  eyeSize 0.13, eyeHeight 1.7, eyeSlope 15, eyeArc 110 (Phacops sp. scan).
- trilobite.build_cephalon: with eyeSolid the heightfield eye becomes a low socket and the primitive is fused at
  (±xe, ye, cheek height). Head builds in ~60 s, single watertight solid (out10/phacopida/head_eyesolid.stl).
- blueprint: eye panel (plan contours + eye circle/arc + transverse section B-B + outward-view lattice) and FOV block.
  Known defects: section B-B is jagged on concatenated meshes (slice the head alone); the outward lattice inset
  did not render on the last sheet (check the try/except in _eye_detail).
- eye_blueprint.py: parameter-drawn eye sheets (plan / outward / section / unrolled lattice / placement) for any preset.
- OPEN: wedge regression — corynexochida 0.8° and phacopida 3.9° by 0.5–0.6 mm^3 contacts on the clipped wedge.
  Restore ~pitch/2 past-the-line reach across the band, or set OVERLAP_TOL to a stated 2 mm^3 floor. Old-wedge
  results (closed at 19–23°) are in out10_v1wedge/.

## Session 4 (9 Sep, ~04:00) — tasks 1 and 2
- **Wedge, resolved by bracketing** (`WEDGE_REACH` 0.15 for seg–seg joints, `WEDGE_REACH_WIDE` 0.5 for head–seg0 and
  last-seg–tail): reach 0 gave 0.6→43 mm^3 seg7–tail contacts from 1°; reach ≥0.25 severed tiled-plate tips at 45°;
  0.15 is clean on both to 10°, and the full-width joints keep full reach so the tail interface is clean to 30°.
  Wedge reach is now part of the ruler and is stated as such. Batch relaunched on it (out10/batch4.log); reach-0
  results archived in out10_v2wedge_reach0/.
- **Eye v1.2**: lobe 1.8 R clipped to the head outline (inside build_eye, before lenses — clipping after broke
  watertightness); holochroal rule: lenses under 0.5 mm at print scale are not built (MIN_PRINT_LENS_MM).
  eyeSolid on asaphida (holochroal, smooth) and odontopleurida (drum, brim 0.15 R, 120 lenses/eye). All three heads
  build watertight (47 s / 7 s / 28 s).

## Session 5 (9 Sep, ~05:00) — batch on the bracketed wedge, run one preset at a time (background jobs die on container restart)
| order | theta/joint | total | limited_by | note |
|---|---|---|---|---|
| corynexochida | 22.59 | 203 | closed | clean, identical to pre-regression |
| proetida | 22.76 | 228 | closed | clean |
| harpetida | 30.32 | 303 | closed | clean; knuckle < 3 mm |
| ptychopariida | 19.78 | 257 | closed (gap 0.36) | clean |
| redlichiida | 21.62 | 324 | closed (gap 0.97) | clean; knuckle < 3 mm |
| asaphida | 19.51 | 176 | anatomy 0.5 mm3 | seg7 (tiled, chord 1.05) severs even at reach 0.15 — NOT a measurement |
| phacopida | — | — | unknown | seg0, seg7 tessellate non-watertight at 44 and 45 deg; measure times out |
| lichida | — | — | — | seg9 OOM-killed in a 4 GB container (builds on a normal machine) |
| agnostida | 0 | — | anatomy | interferes at rest (unchanged) |
| odontopleurida | — | — | — | spine booleans (unchanged) |
Tools: measure_from_stl.py (measure from part STLs built by build_parts_only.py), MEMO_2026-09-09_weaver.md (draft; table to fill).
