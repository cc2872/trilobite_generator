# To-do, in order

Gate A — joints on the sculpt route
- [ ] Confine the ventral bevel and the facet cut to the axial core + pleural rims so short/low segments are
      not eaten; keep flanges exempt. Expect e_max > 0 for the base sculpt and the instrument to become the
      truth on this route (it already is on the legacy parametric route: 0 to the stop, collision past it).
- [ ] Verify renders of the three composite presets at full resolution (memory: decimate for plotting).
- [ ] Rest-pose overlap tolerance: keep 5 mm^3 for slivers; document it in the readout.

Gate B — the library
- [ ] Re-sample patches from the source meshes at full resolution (atlas says where; mesh says how good).
- [ ] Feathering at patch borders; eye catalog with "absent"; brim outline (not only height).
- [ ] Buy/scan: calymenid, asaphid, odontopleurid, agnostid. Register with atlas.py, crop with parts.py.

Gate C — product
- [ ] Background build job + polling for Render's proxy timeout.
- [ ] Rename anything still saying "paradoxidid" (the sculpt is Olenoides, Corynexochida).
- [ ] Grey out knobs that don't apply; tooltips from schema docs.

Gate D — physical
- [ ] Print a 3-segment coupon from the sculpt route; filament pins; confirm swing and stop.
- [ ] Print head + segments + tail; photograph flat and curled.

Gate E — the study (docs/STUDY.md)
